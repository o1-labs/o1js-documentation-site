```ts
function memoizeWitness<T>(type: FlexibleProvable<T>, compute: () => T): T;
```

Defined in: [lib/provable/provable.ts:527](https://github.com/o1-labs/o1js/blob/fca908c243ee8e908efacaf73a882d48759e675a/src/lib/provable/provable.ts#L527)

Like Provable.witness, but memoizes the witness during transaction construction
for reuse by the prover. This is needed to witness non-deterministic values.

## Type Parameters

### T

`T`

## Parameters

### type

[`FlexibleProvable`](../type-aliases/FlexibleProvable.mdx)\<`T`\>

### compute

() => `T`

## Returns

`T`
