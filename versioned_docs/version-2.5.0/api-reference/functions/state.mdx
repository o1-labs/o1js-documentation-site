```ts
function state<A>(type: 
  | ProvableTypePure<A>
  | FlexibleProvablePure<A>): (target: SmartContract & {
  constructor: any;
 }, key: string, _descriptor?: PropertyDescriptor) => void
```

Defined in: [lib/mina/v1/state.ts:100](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/mina/v1/state.ts#L100)

A decorator to use within a zkapp to indicate what will be stored on-chain.
For example, if you want to store a field element `some_state` in a zkapp,
you can use the following in the declaration of your zkapp:

```
@state(Field) some_state = State<Field>();
```

## Type Parameters

• **A**

## Parameters

### type

[`ProvableTypePure`](../type-aliases/ProvableTypePure.mdx)\<`A`\> | [`FlexibleProvablePure`](../type-aliases/FlexibleProvablePure.mdx)\<`A`\>

## Returns

`Function`

### Parameters

#### target

[`SmartContract`](../classes/SmartContract.mdx) & \{
  `constructor`: `any`;
 \}

#### key

`string`

#### \_descriptor?

`PropertyDescriptor`

### Returns

`void`
