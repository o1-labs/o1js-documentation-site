```ts
type Option<T, V> = {
  isSome: Bool;
  value: T;
} & {
  assertNone: void;
  assertSome: T;
  orElse: T;
};
```

Defined in: [lib/provable/option.ts:34](https://github.com/o1-labs/o1js/blob/6d089a824781b83f5d4faa1ceebcb9612ae72411/src/lib/provable/option.ts#L34)

Define an optional version of a provable type.

## Type Declaration

### isSome

```ts
isSome: Bool;
```

### value

```ts
value: T;
```

## Type Declaration

### assertNone()

```ts
assertNone(message?: string): void;
```

#### Parameters

##### message?

`string`

#### Returns

`void`

### assertSome()

```ts
assertSome(message?: string): T;
```

#### Parameters

##### message?

`string`

#### Returns

`T`

### orElse()

```ts
orElse(defaultValue: T | V): T;
```

#### Parameters

##### defaultValue

`T` \| `V`

#### Returns

`T`

## Type Parameters

### T

`T`

### V

`V` = `any`

## Example

```ts
class OptionUInt64 extends Option(UInt64) {}

// create an optional UInt64
let some = OptionUInt64.from(5n);
let none = OptionUInt64.none();

// get back a UInt64
let five: UInt64 = some.assertSome('must have a value');
let zero: UInt64 = none.orElse(0n); // specify a default value
```
