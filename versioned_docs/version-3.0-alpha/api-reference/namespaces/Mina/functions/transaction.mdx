## Call Signature

```ts
function transaction(sender: FeePayerSpec, f: () => Promise<void>): TransactionPromise<false, false>;
```

Defined in: [lib/mina/v1/transaction.ts:712](https://github.com/o1-labs/o1js/blob/f8f5fe4735ab2d63dee8db07178075855e11c71a/src/lib/mina/v1/transaction.ts#L712)

Construct a smart contract transaction. Within the callback passed to this function,
you can call into the methods of smart contracts.

```
let tx = await Mina.transaction(sender, async () => {
  await myZkapp.update();
  await someOtherZkapp.someOtherMethod();
});
```

### Parameters

#### sender

[`FeePayerSpec`](../type-aliases/FeePayerSpec.mdx)

#### f

() => `Promise`\<`void`\>

### Returns

[`TransactionPromise`](../../../../type-aliases/TransactionPromise.mdx)\<`false`, `false`\>

A transaction that can subsequently be submitted to the chain.

## Call Signature

```ts
function transaction(f: () => Promise<void>): TransactionPromise<false, false>;
```

Defined in: [lib/mina/v1/transaction.ts:716](https://github.com/o1-labs/o1js/blob/f8f5fe4735ab2d63dee8db07178075855e11c71a/src/lib/mina/v1/transaction.ts#L716)

Construct a smart contract transaction. Within the callback passed to this function,
you can call into the methods of smart contracts.

```
let tx = await Mina.transaction(sender, async () => {
  await myZkapp.update();
  await someOtherZkapp.someOtherMethod();
});
```

### Parameters

#### f

() => `Promise`\<`void`\>

### Returns

[`TransactionPromise`](../../../../type-aliases/TransactionPromise.mdx)\<`false`, `false`\>

A transaction that can subsequently be submitted to the chain.
