```ts
function checkZkappTransaction(transactionHash: string, blockLength: number): Promise<
  | {
  blockHeight: number;
  failureReason: string[][][];
  success: boolean;
}
  | {
  blockHeight: number;
  failureReason: null;
  success: boolean;
}
  | {
  blockHeight: undefined;
  failureReason: null;
  success: boolean;
}>;
```

Defined in: [lib/mina/v1/fetch.ts:625](https://github.com/o1-labs/o1js/blob/f8f5fe4735ab2d63dee8db07178075855e11c71a/src/lib/mina/v1/fetch.ts#L625)

## Parameters

### transactionHash

`string`

### blockLength

`number` = `20`

## Returns

`Promise`\<
  \| \{
  `blockHeight`: `number`;
  `failureReason`: `string`[][][];
  `success`: `boolean`;
\}
  \| \{
  `blockHeight`: `number`;
  `failureReason`: `null`;
  `success`: `boolean`;
\}
  \| \{
  `blockHeight`: `undefined`;
  `failureReason`: `null`;
  `success`: `boolean`;
\}\>
