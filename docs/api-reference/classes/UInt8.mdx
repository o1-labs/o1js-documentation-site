Defined in: [lib/provable/int.ts:1511](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1511)

A 8 bit unsigned integer with values ranging from 0 to 255.

## Extends

- \{
  `value`: [`Field`](Field.mdx);
 \}

## Constructors

### new UInt8()

```ts
new UInt8(x: number | bigint | FieldVar | UInt8): UInt8
```

Defined in: [lib/provable/int.ts:1522](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1522)

Create a [UInt8](UInt8.mdx) from a bigint or number.
The max value of a [UInt8](UInt8.mdx) is `2^8 - 1 = 255`.

**Warning**: Cannot overflow past 255, an error is thrown if the result is greater than 255.

#### Parameters

##### x

`number` | `bigint` | `FieldVar` | [`UInt8`](UInt8.mdx)

#### Returns

[`UInt8`](UInt8.mdx)

#### Overrides

```ts
Struct({
  value: Field,
}).constructor
```

## Properties

### value

```ts
value: Field = Field;
```

Defined in: [lib/provable/int.ts:1512](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1512)

#### Inherited from

```ts
Struct({
  value: Field,
}).value
```

***

### \_isStruct

```ts
static _isStruct: true;
```

Defined in: [lib/provable/types/struct.ts:133](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/types/struct.ts#L133)

#### Inherited from

```ts
Struct({
  value: Field,
})._isStruct
```

***

### empty()

```ts
static empty: () => {
  value: Field;
};
```

Defined in: [lib/provable/types/struct.ts:143](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/types/struct.ts#L143)

#### Returns

```ts
{
  value: Field;
}
```

##### value

```ts
value: Field = Field;
```

#### Inherited from

```ts
Struct({
  value: Field,
}).empty
```

***

### fromFields()

```ts
static fromFields: (fields: Field[]) => {
  value: Field;
};
```

Defined in: [lib/provable/types/provable-intf.ts:115](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/types/provable-intf.ts#L115)

#### Parameters

##### fields

[`Field`](Field.mdx)[]

#### Returns

```ts
{
  value: Field;
}
```

##### value

```ts
value: Field = Field;
```

#### Inherited from

```ts
Struct({
  value: Field,
}).fromFields
```

***

### fromJSON()

```ts
static fromJSON: (x: {
  value: string;
 }) => {
  value: Field;
};
```

Defined in: [lib/provable/types/struct.ts:142](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/types/struct.ts#L142)

#### Parameters

##### x

###### value

`string` = `Field`

#### Returns

```ts
{
  value: Field;
}
```

##### value

```ts
value: Field = Field;
```

#### Inherited from

```ts
Struct({
  value: Field,
}).fromJSON
```

***

### NUM\_BITS

```ts
static NUM_BITS: number = 8;
```

Defined in: [lib/provable/int.ts:1514](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1514)

***

### toAuxiliary()

```ts
static toAuxiliary: (value?: {
  value: Field;
 }) => any[];
```

Defined in: [lib/provable/types/provable-intf.ts:47](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/types/provable-intf.ts#L47)

A function that takes `value` (optional), an element of type `T`, as argument and
returns an array of any type that make up the "auxiliary" (non-provable) data of `value`.

#### Parameters

##### value?

the element of type `T` to generate the auxiliary data array from, optional.
If not provided, a default value for auxiliary data is returned.

###### value

[`Field`](Field.mdx) = `Field`

#### Returns

`any`[]

An array of any type describing how this `T` element is made up of "auxiliary" (non-provable) data.

#### Inherited from

```ts
Struct({
  value: Field,
}).toAuxiliary
```

***

### toCanonical()?

```ts
static optional toCanonical: (x: {
  value: Field;
 }) => {
  value: Field;
};
```

Defined in: [lib/provable/types/provable-intf.ts:104](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/types/provable-intf.ts#L104)

Optional method which transforms a provable type into its canonical representation.

This is needed for types that have multiple representations of the same underlying value,
and might even not have perfect completeness for some of those representations.

An example is the `ForeignField` class, which allows non-native field elements to exist in unreduced form.
The unreduced form is not perfectly complete, for example, addition of two unreduced field elements can cause a prover error.

Specific protocols need to be able to protect themselves against incomplete operations at all costs.
For example, when using actions and reducer, the reducer must be able to produce a proof regardless of the input action.
`toCanonical()` converts any input into a safe form and enables us to handle cases like this generically.

Note: For most types, this method is the identity function.
The identity function will also be used when the `toCanonical()` is not present on a type.

#### Parameters

##### x

###### value

[`Field`](Field.mdx) = `Field`

#### Returns

```ts
{
  value: Field;
}
```

##### value

```ts
value: Field = Field;
```

#### Inherited from

```ts
Struct({
  value: Field,
}).toCanonical
```

***

### toFields()

```ts
static toFields: (value: {
  value: Field;
 }) => Field[];
```

Defined in: [lib/provable/types/provable-intf.ts:36](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/types/provable-intf.ts#L36)

A function that takes `value`, an element of type `T`, as argument and returns
an array of [Field](Field.mdx) elements that make up the provable data of `value`.

#### Parameters

##### value

the element of type `T` to generate the [Field](Field.mdx) array from.

###### value

[`Field`](Field.mdx) = `Field`

#### Returns

[`Field`](Field.mdx)[]

A [Field](Field.mdx) array describing how this `T` element is made up of [Field](Field.mdx) elements.

#### Inherited from

```ts
Struct({
  value: Field,
}).toFields
```

***

### toJSON()

```ts
static toJSON: (x: {
  value: Field;
 }) => {
  value: string;
};
```

Defined in: [lib/provable/types/struct.ts:141](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/types/struct.ts#L141)

#### Parameters

##### x

###### value

[`Field`](Field.mdx) = `Field`

#### Returns

```ts
{
  value: string;
}
```

##### value

```ts
value: string = Field;
```

#### Inherited from

```ts
Struct({
  value: Field,
}).toJSON
```

***

### toValue()

```ts
static toValue: (x: {
  value: Field;
 }) => {
  value: bigint;
};
```

Defined in: [lib/provable/types/provable-intf.ts:81](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/types/provable-intf.ts#L81)

Convert provable type to a normal JS type.

#### Parameters

##### x

###### value

[`Field`](Field.mdx) = `Field`

#### Returns

```ts
{
  value: bigint;
}
```

##### value

```ts
value: bigint = Field;
```

#### Inherited from

```ts
Struct({
  value: Field,
}).toValue
```

***

### Unsafe

```ts
static Unsafe: {
  fromField: UInt8;
};
```

Defined in: [lib/provable/int.ts:1528](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1528)

#### fromField()

Create a [UInt8](UInt8.mdx) from a [Field](../variables/Field.mdx) without constraining its range.

**Warning**: This is unsafe, because it does not prove that the input [Field](../variables/Field.mdx) actually fits in 8 bits.\
Only use this if you know what you are doing, otherwise use the safe [UInt8.from](UInt8.mdx#from).

##### Parameters

###### x

[`Field`](Field.mdx)

##### Returns

[`UInt8`](UInt8.mdx)

## Accessors

### one

#### Get Signature

```ts
get static one(): UInt8
```

Defined in: [lib/provable/int.ts:1549](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1549)

Static method to create a [UInt8](UInt8.mdx) with value `1`.

##### Returns

[`UInt8`](UInt8.mdx)

***

### zero

#### Get Signature

```ts
get static zero(): UInt8
```

Defined in: [lib/provable/int.ts:1543](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1543)

Static method to create a [UInt8](UInt8.mdx) with value `0`.

##### Returns

[`UInt8`](UInt8.mdx)

## Methods

### add()

```ts
add(y: number | bigint | UInt8): UInt8
```

Defined in: [lib/provable/int.ts:1565](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1565)

Add a [UInt8](UInt8.mdx) to another [UInt8](UInt8.mdx) without allowing overflow.

#### Parameters

##### y

`number` | `bigint` | [`UInt8`](UInt8.mdx)

#### Returns

[`UInt8`](UInt8.mdx)

#### Example

```ts
const x = UInt8.from(3);
const sum = x.add(5);
sum.assertEquals(8);
```

#### Throws

if the result is greater than 255.

***

### assertEquals()

```ts
assertEquals(y: number | bigint | UInt8, message?: string): void
```

Defined in: [lib/provable/int.ts:1811](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1811)

Assert that this [UInt8](UInt8.mdx) is equal another [UInt8](UInt8.mdx) value.

**Important**: If an assertion fails, the code throws an error.

#### Parameters

##### y

the [UInt8](UInt8.mdx) value to compare & assert with this [UInt8](UInt8.mdx).

`number` | `bigint` | [`UInt8`](UInt8.mdx)

##### message?

`string`

a string error message to print if the assertion fails, optional.

#### Returns

`void`

***

### assertGreaterThan()

```ts
assertGreaterThan(y: number | bigint | UInt8, message?: string): void
```

Defined in: [lib/provable/int.ts:1787](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1787)

Assert that this [UInt8](UInt8.mdx) is greater than another [UInt8](UInt8.mdx) value.

**Important**: If an assertion fails, the code throws an error.

#### Parameters

##### y

the [UInt8](UInt8.mdx) value to compare & assert with this [UInt8](UInt8.mdx).

`number` | `bigint` | [`UInt8`](UInt8.mdx)

##### message?

`string`

a string error message to print if the assertion fails, optional.

#### Returns

`void`

***

### assertGreaterThanOrEqual()

```ts
assertGreaterThanOrEqual(y: UInt8, message?: string): void
```

Defined in: [lib/provable/int.ts:1799](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1799)

Assert that this [UInt8](UInt8.mdx) is greater than or equal to another [UInt8](UInt8.mdx) value.

**Important**: If an assertion fails, the code throws an error.

#### Parameters

##### y

[`UInt8`](UInt8.mdx)

the [UInt8](UInt8.mdx) value to compare & assert with this [UInt8](UInt8.mdx).

##### message?

`string`

a string error message to print if the assertion fails, optional.

#### Returns

`void`

***

### assertLessThan()

```ts
assertLessThan(y: number | bigint | UInt8, message?: string): void
```

Defined in: [lib/provable/int.ts:1715](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1715)

Assert that this [UInt8](UInt8.mdx) is less than another [UInt8](UInt8.mdx) value.

**Important**: If an assertion fails, the code throws an error.

#### Parameters

##### y

the [UInt8](UInt8.mdx) value to compare & assert with this [UInt8](UInt8.mdx).

`number` | `bigint` | [`UInt8`](UInt8.mdx)

##### message?

`string`

a string error message to print if the assertion fails, optional.

#### Returns

`void`

***

### assertLessThanOrEqual()

```ts
assertLessThanOrEqual(y: number | bigint | UInt8, message?: string): void
```

Defined in: [lib/provable/int.ts:1737](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1737)

Assert that this [UInt8](UInt8.mdx) is less than or equal to another [UInt8](UInt8.mdx) value.

**Important**: If an assertion fails, the code throws an error.

#### Parameters

##### y

the [UInt8](UInt8.mdx) value to compare & assert with this [UInt8](UInt8.mdx).

`number` | `bigint` | [`UInt8`](UInt8.mdx)

##### message?

`string`

a string error message to print if the assertion fails, optional.

#### Returns

`void`

***

### div()

```ts
div(y: number | bigint | UInt8): UInt8
```

Defined in: [lib/provable/int.ts:1618](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1618)

Divide a [UInt8](UInt8.mdx) by another [UInt8](UInt8.mdx).
This is integer division that rounds down.

#### Parameters

##### y

`number` | `bigint` | [`UInt8`](UInt8.mdx)

#### Returns

[`UInt8`](UInt8.mdx)

#### Example

```ts
const x = UInt8.from(7);
const quotient = x.div(2);
quotient.assertEquals(3);
```

***

### divMod()

```ts
divMod(y: number | bigint | UInt8): {
  quotient: UInt8;
  remainder: UInt8;
}
```

Defined in: [lib/provable/int.ts:1645](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1645)

Get the quotient and remainder of a [UInt8](UInt8.mdx) divided by another [UInt8](UInt8.mdx):

`x == y * q + r`, where `0 <= r < y`.

#### Parameters

##### y

a [UInt8](UInt8.mdx) to get the quotient and remainder of another [UInt8](UInt8.mdx).

`number` | `bigint` | [`UInt8`](UInt8.mdx)

#### Returns

```ts
{
  quotient: UInt8;
  remainder: UInt8;
}
```

The quotient `q` and remainder `r`.

##### quotient

```ts
quotient: UInt8;
```

##### remainder

```ts
remainder: UInt8;
```

***

### greaterThan()

```ts
greaterThan(y: number | bigint | UInt8): Bool
```

Defined in: [lib/provable/int.ts:1761](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1761)

Check if this [UInt8](UInt8.mdx) is greater than another [UInt8](UInt8.mdx).
Returns a [Bool](../variables/Bool.mdx).

#### Parameters

##### y

`number` | `bigint` | [`UInt8`](UInt8.mdx)

#### Returns

[`Bool`](Bool.mdx)

#### Example

```ts
// 5 > 3
UInt8.from(5).greaterThan(3);
```

***

### greaterThanOrEqual()

```ts
greaterThanOrEqual(y: number | bigint | UInt8): Bool
```

Defined in: [lib/provable/int.ts:1775](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1775)

Check if this [UInt8](UInt8.mdx) is greater than or equal another [UInt8](UInt8.mdx) value.
Returns a [Bool](../variables/Bool.mdx).

#### Parameters

##### y

`number` | `bigint` | [`UInt8`](UInt8.mdx)

#### Returns

[`Bool`](Bool.mdx)

#### Example

```ts
// 3 >= 3
UInt8.from(3).greaterThanOrEqual(3);
```

***

### isConstant()

```ts
isConstant(): boolean
```

Defined in: [lib/provable/int.ts:1905](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1905)

#### Returns

`boolean`

***

### lessThan()

```ts
lessThan(y: number | bigint | UInt8): Bool
```

Defined in: [lib/provable/int.ts:1699](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1699)

Check if this [UInt8](UInt8.mdx) is less than another [UInt8](UInt8.mdx) value.
Returns a [Bool](../variables/Bool.mdx).

#### Parameters

##### y

`number` | `bigint` | [`UInt8`](UInt8.mdx)

#### Returns

[`Bool`](Bool.mdx)

#### Example

```ts
UInt8.from(2).lessThan(UInt8.from(3));
```

***

### lessThanOrEqual()

```ts
lessThanOrEqual(y: number | bigint | UInt8): Bool
```

Defined in: [lib/provable/int.ts:1682](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1682)

Check if this [UInt8](UInt8.mdx) is less than or equal to another [UInt8](UInt8.mdx) value.
Returns a [Bool](../variables/Bool.mdx).

#### Parameters

##### y

`number` | `bigint` | [`UInt8`](UInt8.mdx)

#### Returns

[`Bool`](Bool.mdx)

#### Example

```ts
UInt8.from(3).lessThanOrEqual(UInt8.from(5));
```

***

### mod()

```ts
mod(y: number | bigint | UInt8): UInt8
```

Defined in: [lib/provable/int.ts:1632](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1632)

Get the remainder a [UInt8](UInt8.mdx) of division of another [UInt8](UInt8.mdx).

#### Parameters

##### y

`number` | `bigint` | [`UInt8`](UInt8.mdx)

#### Returns

[`UInt8`](UInt8.mdx)

#### Example

```ts
const x = UInt8.from(50);
const mod = x.mod(30);
mod.assertEquals(20);
```

***

### mul()

```ts
mul(y: number | bigint | UInt8): UInt8
```

Defined in: [lib/provable/int.ts:1601](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1601)

Multiply a [UInt8](UInt8.mdx) by another [UInt8](UInt8.mdx) without allowing overflow.

#### Parameters

##### y

`number` | `bigint` | [`UInt8`](UInt8.mdx)

#### Returns

[`UInt8`](UInt8.mdx)

#### Example

```ts
const x = UInt8.from(3);
const product = x.mul(5);
product.assertEquals(15);
```

#### Throws

if the result is greater than 255.

***

### sub()

```ts
sub(y: number | bigint | UInt8): UInt8
```

Defined in: [lib/provable/int.ts:1583](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1583)

Subtract a [UInt8](UInt8.mdx) from another [UInt8](UInt8.mdx) without allowing underflow.

#### Parameters

##### y

`number` | `bigint` | [`UInt8`](UInt8.mdx)

#### Returns

[`UInt8`](UInt8.mdx)

#### Example

```ts
const x = UInt8.from(8);
const difference = x.sub(5);
difference.assertEquals(3);
```

#### Throws

if the result is less than 0.

***

### toBigInt()

```ts
toBigInt(): bigint
```

Defined in: [lib/provable/int.ts:1839](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1839)

Serialize the [UInt8](UInt8.mdx) to a bigint.

**Warning**: This operation is not provable.

#### Returns

`bigint`

***

### toBits()

```ts
toBits(length: number): Bool[]
```

Defined in: [lib/provable/int.ts:1922](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1922)

Returns an array of [Bool](../variables/Bool.mdx) elements representing [little endian binary representation](https://en.wikipedia.org/wiki/Endianness) of this [UInt8](UInt8.mdx) element.

If you use the optional `length` argument, proves that the UInt8 element fits in `length` bits.
The `length` has to be between 0 and 8 and the method throws if it isn't.

**Warning**: The cost of this operation in a zk proof depends on the `length` you specify,
which by default is 8 bits. Prefer to pass a smaller `length` if possible.

#### Parameters

##### length

`number` = `8`

the number of bits to fit the element. If the element does not fit in `length` bits, the functions throws an error.

#### Returns

[`Bool`](Bool.mdx)[]

An array of [Bool](../variables/Bool.mdx) element representing little endian binary representation of this [UInt8](UInt8.mdx).

***

### toNumber()

```ts
toNumber(): number
```

Defined in: [lib/provable/int.ts:1830](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1830)

Serialize the [UInt8](UInt8.mdx) to a number.

**Warning**: This operation is not provable.

#### Returns

`number`

***

### toString()

```ts
toString(): string
```

Defined in: [lib/provable/int.ts:1821](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1821)

Serialize the [UInt8](UInt8.mdx) to a string, e.g. for printing.

**Warning**: This operation is not provable.

#### Returns

`string`

***

### toUInt32()

```ts
toUInt32(): UInt32
```

Defined in: [lib/provable/int.ts:1859](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1859)

Turns a [UInt8](UInt8.mdx) into a [UInt32](UInt32.mdx).

#### Returns

[`UInt32`](UInt32.mdx)

***

### toUInt64()

```ts
toUInt64(): UInt64
```

Defined in: [lib/provable/int.ts:1866](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1866)

Turns a [UInt8](UInt8.mdx) into a [UInt64](UInt64.mdx).

#### Returns

[`UInt64`](UInt64.mdx)

***

### check()

```ts
static check(x: 
  | Field
  | {
  value: Field;
 }): void
```

Defined in: [lib/provable/int.ts:1847](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1847)

[Provable.check](../type-aliases/Provable.mdx#check) for [UInt8](UInt8.mdx).
Proves that the input is in the [0, 255] range.

#### Parameters

##### x

[`Field`](Field.mdx) | \{
`value`: [`Field`](Field.mdx);
\}

#### Returns

`void`

#### Overrides

```ts
Struct({
  value: Field,
}).check
```

***

### from()

```ts
static from(x: 
  | number
  | bigint
  | Field
  | UInt64
  | UInt32
  | UInt8): UInt8
```

Defined in: [lib/provable/int.ts:1880](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1880)

Creates a new [UInt8](UInt8.mdx).

#### Parameters

##### x

`number` | `bigint` | [`Field`](Field.mdx) | [`UInt64`](UInt64.mdx) | [`UInt32`](UInt32.mdx) | [`UInt8`](UInt8.mdx)

#### Returns

[`UInt8`](UInt8.mdx)

***

### fromBits()

```ts
static fromBits(bits: (boolean | Bool)[]): UInt8
```

Defined in: [lib/provable/int.ts:1946](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1946)

Convert a bit array into a [UInt8](UInt8.mdx) element using [little endian binary representation](https://en.wikipedia.org/wiki/Endianness)

The method throws if the given bits do not fit in a single UInt8 element. In this case, no more than 8 bits are allowed.

**Important**: If the given `bits` array is an array of `booleans` or [Bool](../variables/Bool.mdx) elements that all are `constant`,
 the resulting [UInt8](UInt8.mdx) element will be a constant as well. Or else, if the given array is a mixture of constants and variables of [Bool](../variables/Bool.mdx) type,
 the resulting [UInt8](UInt8.mdx) will be a variable as well.

#### Parameters

##### bits

(`boolean` \| [`Bool`](Bool.mdx))[]

An array of [Bool](../variables/Bool.mdx) or `boolean` type.

#### Returns

[`UInt8`](UInt8.mdx)

A [UInt8](UInt8.mdx) element matching the [little endian binary representation](https://en.wikipedia.org/wiki/Endianness) of the given `bits` array.

***

### fromValue()

```ts
static fromValue(x: 
  | number
  | UInt8
  | {
  value: string | number | bigint | Field;
 }): UInt8
```

Defined in: [lib/provable/int.ts:1891](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1891)

#### Parameters

##### x

`number` | [`UInt8`](UInt8.mdx) | \{
`value`: `string` \| `number` \| `bigint` \| [`Field`](Field.mdx);
\}

#### Returns

[`UInt8`](UInt8.mdx)

#### Overrides

```ts
Struct({
  value: Field,
}).fromValue
```

***

### MAXINT()

```ts
static MAXINT(): UInt8
```

Defined in: [lib/provable/int.ts:1873](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1873)

Creates a [UInt8](UInt8.mdx) with a value of 255.

#### Returns

[`UInt8`](UInt8.mdx)

***

### sizeInFields()

```ts
static sizeInFields(): number
```

Defined in: [lib/provable/types/provable-intf.ts:66](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/types/provable-intf.ts#L66)

Return the size of the `T` type in terms of [Field](Field.mdx) type, as [Field](Field.mdx) is the primitive type.

#### Returns

`number`

A `number` representing the size of the `T` type in terms of [Field](Field.mdx) type.

#### Inherited from

```ts
Struct({
  value: Field,
}).sizeInFields
```

***

### toInput()

```ts
static toInput(x: {
  value: Field;
 }): HashInput
```

Defined in: [lib/provable/int.ts:1852](https://github.com/o1-labs/o1js/blob/df1ff77af12d7cbbed4a6167f02815fad4f0dceb/src/lib/provable/int.ts#L1852)

#### Parameters

##### x

###### value

[`Field`](Field.mdx)

#### Returns

`HashInput`

#### Overrides

```ts
Struct({
  value: Field,
}).toInput
```
