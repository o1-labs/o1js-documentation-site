```ts
function hash(json: string | ZkappCommand): Promise<string>;
```

Defined in: [lib/mina/v1/transaction.ts:118](https://github.com/o1-labs/o1js/blob/cc18a919fe40be152afb1b603c57d74a9b2225b0/src/lib/mina/v1/transaction.ts#L118)

Computes the hash of a transaction represented as a JSON object or JSON string.
This hash serves as a unique identifier for the transaction and is essential for tracking
and verifying transactions on the Mina blockchain.

## Parameters

### json

`string` | `ZkappCommand`

## Returns

`Promise`\<`string`\>
