```ts
function checkZkappTransaction(transactionHash: string, blockLength: number): Promise<
  | {
  failureReason: string[][][];
  success: boolean;
}
  | {
  failureReason: null;
  success: boolean;
}>;
```

Defined in: [lib/mina/v1/fetch.ts:519](https://github.com/o1-labs/o1js/blob/fca908c243ee8e908efacaf73a882d48759e675a/src/lib/mina/v1/fetch.ts#L519)

## Parameters

### transactionHash

`string`

### blockLength

`number` = `20`

## Returns

`Promise`\<
  \| \{
  `failureReason`: `string`[][][];
  `success`: `boolean`;
\}
  \| \{
  `failureReason`: `null`;
  `success`: `boolean`;
\}\>
